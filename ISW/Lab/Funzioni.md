Sequenze di comandi a cui diamo un nome, argomenti, e forse un valore di ritorno.

```Python
def nome_funzione(param1, param2=10):
	istruzioni
	[return]
```

Lì, `param2=10` dà un valore default se il parametro non viene passato.